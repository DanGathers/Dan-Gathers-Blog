include ../partials/header.pug
title New Blog

div
    h1 Add New Blog Post
    div.input-box
        form(action="/blogs" method="POST")
        p Title
            input(type="text" name="title" placeholder="Title of Blog")
        p Image
            input(type="text" name="image" placeholder="Add Image Here")
        p Post
            textarea(name="blogpost" placeholder="Add a new blog post here...")